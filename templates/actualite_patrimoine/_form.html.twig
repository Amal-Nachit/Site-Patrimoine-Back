{{ form_start(form) }}
    <div class="space-y-4">
        {{ form_widget(form, {
            'attr': {
                'class': 'form-control'
            }
        }) }}
    </div>
    <button class="bg-green-600 text-white font-semibold py-2 px-4 rounded-lg shadow-md hover:bg-green-700 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-opacity-50 transition duration-150 ease-in-out mt-4">
        {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
