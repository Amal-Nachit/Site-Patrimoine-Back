{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <div class="relative min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8 bg-cover"
         style="background-image: url({{ asset('/Background/BgImage.jpg') }});">
        <form method="post" class="bg-white bg-opacity-50 shadow-md rounded-lg px-8 pt-6 pb-8 mb-4 max-w-sm w-full">
            {% if error %}
                <div class="bg-red-200 text-red-700 p-2 rounded mb-4">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            {% if app.user %}
                <div class="mb-3 text-gray-700">
                    Bonjour ! <a href="{{ path('app_logout') }}" class="text-blue-600 underline">Logout</a>
                </div>
            {% endif %}

            <div class="mb-4">
                <label for="username" class="block text-md font-semibold mt-6 mb-2">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail"
                       class="shadow appearance-none border border-gray-400 rounded w-full py-2 px-3  leading-tight focus:outline-none focus:shadow-outline focus:border-blue-300"
                       autocomplete="email" required autofocus>
            </div>
            <div class="mb-6">
                <label for="password" class="block text-md font-semibold mt-6 mb-2">Mot de passe</label>
                <input type="password" name="password" id="inputPassword"
                       class="shadow appearance-none border border-gray-400 rounded w-full py-2 px-3 leading-tight focus:outline-none focus:shadow-outline focus:border-blue-300"
                       autocomplete="current-password" required>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <div class="flex items-center justify-between">
                <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                        type="submit">
                    Se connecter
                </button>
               <a href="{{ path('app_forgotten_password') }}" class="inline-block align-baseline font-bold text-sm text-blue-500 hover:text-blue-800">Mot de passe oublié</a>
            </div>
        </form>
    </div>
{% endblock %}
